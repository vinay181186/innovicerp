Switches views within one page. For open-page browser tabs use PageTabs; for filter counts use StatStrip.
```jsx
<TabStrip activeKey="inbox" tabs={[{ key: 'inbox', label: 'Inbox', count: 4 }, { key: 'all', label: 'All Tasks', note: '(Admin)' }]} />
```