App header navigation — label-only module buttons open a white menu card with one column per group.
```jsx
<TopNav logoSrc="assets/innovic-logo.jpeg" sections={SECTIONS} activeKey="sales" openKey={open} onToggle={setOpen} currentPage="SO Master" right={<Input className="gs-input" placeholder="Search…" />} />
```