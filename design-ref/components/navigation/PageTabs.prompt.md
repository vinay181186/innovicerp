Open-pages strip (one tab per visited module page) with × close.
```jsx
<PageTabs tabs={[{ key: 'home', label: 'Dashboard', icon: '📊' }, { key: 'so', label: 'SO Master', icon: '📋' }]} activeKey="so" />
```