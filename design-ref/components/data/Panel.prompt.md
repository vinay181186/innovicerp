The data container for every screen section — tables, forms, lists live inside one.
```jsx
<Panel title="Needs Attention" actions={<Badge tone="red">3 critical</Badge>}>…</Panel>
<Panel title="Line items" bodyPadding={0}><DataTable … /></Panel>
```