Confirmation / exit guard. Default copy is the product's.
```jsx
<ConfirmDialog onConfirm={leave} onCancel={stay} />
<ConfirmDialog title="Delete SO IN-SO-26-0142?" message="This soft-deletes the whole order." confirmLabel="Delete" />
```