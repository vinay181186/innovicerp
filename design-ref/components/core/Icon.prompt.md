Control icon for buttons and row actions — the ONE icon source for controls (never emoji on a control).
```jsx
<Button variant="ghost" size="sm" icon={<Icon name="pencil" size={13} />}>Edit</Button>
<Button variant="ghost" size="sm" icon={<Icon name="printer" size={13} />}>Print</Button>
```
Canonical icon per action: View eye · Edit pencil · Delete trash-2 · Export download · Import upload · Print printer · Add plus · Back arrow-left · Stop square · Assign user-round · Attach paperclip · Refresh refresh-cw · Loading loader-2.