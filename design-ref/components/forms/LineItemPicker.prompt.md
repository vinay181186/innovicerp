Per-line item cell for document line editors (GRN, PO…). Renders two form-grp siblings — place inside a FormGrid.
```jsx
<FormGrid><LineItemPicker code={c} name={n} items={ITEMS} onChange={(v) => …} /></FormGrid>
```
Matched name is read-only on --bg4 with --text3 (derived look).