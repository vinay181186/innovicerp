Checkbox / radio row (access-control ticks, report builder options, "Can download drawing files").
```jsx
<CheckField label="Can download drawing files" defaultChecked />
<CheckField type="radio" name="disp" label="Rework" />
```