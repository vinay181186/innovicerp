# Phase 2 - adversarial review findings

Total: 108 findings across 7 groups.

## OTHER  (5)

- **[medium]** modules/so-status/routes/index.tsx - NOT this group's doing, but flagging upstream: `git status --short` in C:/Innovic_projects/innovic-erp/wt-ui-overhaul shows 4 dirty files under apps/web/src/modules/ (so-status/routes/index.tsx +126 lines, so-planning/components/create-plan-modal.tsx, so-planning/components/edit-plan-modal.tsx, purchase-orders/components/po-form-css.ts) plus components/shared/breadcrumbs.tsx and open-tabs-bar.tsx. The hard rules forbid Phase 2 modifying any existing screen under modules/. The diffs are the Foundation group's Tailwind-namespace removal (e.g. `[&_input]:bg-innovic-blue3` → `.field-tint`), so the core group is clean — but the rule is being broken by someone and needs sign-off.
- **[medium]** C:/Innovic_projects/innovic-erp/wt-ui-overhaul - Review question 7, scope: this group is CLEAN - `git status --short` shows `?? apps/web/src/ui/` as the only untracked path, no import from `@/modules/**` or `@innovic/shared` exists in any of the 8 files (verified by grep), nothing under api/, packages/shared/ or lib/print/ is touched, and no Tailwind utility class appears anywhere (the only classNames are house-vocabulary: panel, panel-hdr, panel-title, panel-body, mono, fw-700, text2, text3, cyan, badge, b-blue, td-code, td-left, innovic-table tbl-grid tbl-compact, prog-wrap, prog-bar, empty-state, dash-link, dash-cell, form-grp, form-label). HOWEVER the worktree as a whole has modifications OUTSIDE the permitted set: apps/web/src/modules/so-status/routes/index.tsx (+106/-20), modules/so-planning/components/{create,edit}-plan-modal.tsx, modules/purchase-orders/components/po-form-css.ts, components/shared/breadcrumbs.tsx, components/shared/open-tabs-bar.tsx. The Phase-2 hard rule says "Do NOT modify any existing screen under apps/web/src/modules/". Reading the diffs, they are all --content-pad / --topbar-height token derivation, a `.field-tint` class replacing a Tailwind arbitrary variant, and prettier reflow - i.e. the Foundation task's Tailwind-removal work, not this group's. Confirm the Foundation brief authorised touching those five files before the gate; if it did not, so-status/routes/index.tsx is the one that changes real layout (height calc and gutter bleed) and needs eyeballing.
- **[medium]** C:/Innovic_projects/innovic-erp/wt-ui-overhaul (worktree, not this agent's files) - `git status --short` shows 10 modified files OUTSIDE apps/web/src/ui/, four of them under modules/ -- which the hard rules forbid: modules/purchase-orders/components/po-form-css.ts (+14), modules/so-planning/components/create-plan-modal.tsx (+8), modules/so-planning/components/edit-plan-modal.tsx (+8), and modules/so-status/routes/index.tsx (126 changed lines). Plus components/shared/breadcrumbs.tsx, components/shared/open-tabs-bar.tsx, index.css, styles/tokens.css, styles/innovic-theme.css (826 changed lines), tailwind.config.ts. NOT attributable to the layout agent: every one of those files has an mtime of 17:39-17:57, while apps/web/src/ui/ was created at 18:03 and all 9 layout files were written at 18:18. The layout agent's claim ('nothing outside apps/web/src/ui/layout/ was touched') is verified true. Raising it so the parent chases whoever did touch modules/.
- **[medium]** modules/ - NOT this agent's work, but flagging upward: git status --short shows four modules/ files dirty - purchase-orders/components/po-form-css.ts, so-planning/components/create-plan-modal.tsx, so-planning/components/edit-plan-modal.tsx, so-status/routes/index.tsx (+126 lines). The diffs are Tailwind-removal ([&_input]:bg-innovic-blue3 -> .field-tint), i.e. the foundation group's edits. The hard rule forbids modifying any existing screen under modules/. Navigation's own footprint is confined to apps/web/src/ui/navigation/ (untracked).
- **[info]** C:/Innovic_projects/innovic-erp/wt-ui-overhaul (worktree root) - SCOPE - NOT THIS GROUP'S, but the parent needs to know. `git status --short` shows ten MODIFIED tracked files, six of them under paths the phase rules forbid: apps/web/src/modules/so-status/routes/index.tsx (+126 lines), modules/so-planning/components/{create,edit}-plan-modal.tsx, modules/purchase-orders/components/po-form-css.ts, components/shared/breadcrumbs.tsx, components/shared/open-tabs-bar.tsx - plus styles/innovic-theme.css (826 lines changed), styles/tokens.css, index.css, tailwind.config.ts. I grepped all of those diffs for 'ui/forms', 'SearchInput' and 'FormField': ZERO hits, so the forms-basic group did not make them (they are the Foundation group's token/CSS normalisation). The forms group itself added only untracked apps/web/src/ui/ files. No action against forms-basic; verify the modules/ edits against the Foundation task's named file list.

## core  (11)

- **[high]** ui/core/Button.tsx - Line 38: PILL_STYLE sets `height: 'auto'`, which the design reference never does. design-ref/components/core/Button.jsx overrides only borderRadius/padding/fontSize for a pill and leaves `.btn`'s `height: var(--control-h)` (28px) in place; design-ref/components/layout/StatusPills.jsx does the same. design-ref/README.md's rules block says 'Control height: buttons, inputs, selects and search share --control-h 28px (sm 24px)'. Failure: `<Button pill>All</Button>` in a list toolbar renders ~21px tall instead of 28px and no longer aligns with the SearchInput and ghost buttons beside it — exactly the row design-ref/components/core/core.card.html:14 renders. The agent's stated rationale is inverted: the reference chip IS --control-h tall.
- **[medium]** ui/core/StatusBadge.tsx - Line 175: `MAP[kind][key] ?? 'grey'` drops the reference's defensive fallback. design-ref/components/core/StatusBadge.jsx does `const m = MAP[kind] || MAP.doc`. Failure: any `kind` that is not a literal — widened to `string` from data, passed via `as StatusKind`, or a new enum member added before the map is updated — makes `MAP[kind]` undefined and throws `TypeError: Cannot read properties of undefined` at render, crashing the page, where the reference silently degrades to the generic `doc` map.
- **[medium]** ui/core/StatusBadge.tsx - Line 169: no null/empty handling, so StatusBadge cannot replace modules/nc-register/components/nc-disposition-badge.tsx without a behaviour regression. That file returns `<span className="text3">—</span>` when `disposition` is null. Failure: `<StatusBadge kind="ncdisp" status={nc.disposition} />` with a null disposition runs `String(null)` and renders a grey badge reading the literal text 'NULL' instead of an em dash. The same applies to every nullable status column. The group's own notes flagged the ncdisp LABEL divergence but not this branch.
- **[medium]** ui/core/Button.tsx - Line 71: `className="animate-spin"` is a Tailwind utility class inside ui/core, against the brief's explicit 'Do NOT introduce Tailwind utilities' house rule. It resolves today (core Tailwind, not one of the `innovic.*`/`fontSize.innovic-*` namespaces the Foundation group deleted in apps/web/tailwind.config.ts), but it couples the primitive library to Tailwind surviving in a worktree where Tailwind is actively being stripped.
- **[medium]** ui/core/Button.tsx - Line 24: the 'icon-only buttons always carry a title' rule (design-ref/README.md, Iconography) is not enforced. `iconOnly` is a public prop on ButtonProps and `title` remains optional there (inherited from ButtonHTMLAttributes), so `<Button iconOnly icon={<Icon name="eye" />} />` with no title compiles cleanly. IconButton enforces it, but nothing stops a Phase 4 migrator bypassing IconButton across the 229 `className="btn ` call sites the audit counted.
- **[low]** ui/core/SyncDot.tsx - Lines 48-49: `role="status"` and `aria-label={STATE_TEXT[state]}` are set unconditionally, neither of which the reference SyncDot.jsx has. Failure: with the documented usage `<SyncDot state="ok" label="SYNCED" />` the aria-label 'Synced' overrides the visible caption for screen readers (visible-label / accessible-name mismatch), and because role="status" is an ARIA live region the header dot re-announces on every mount and route change.
- **[low]** ui/core/Icon.tsx - Lines 126-128: `title` is mapped to `role="img"` + `aria-label` instead of the reference's SVG `<title>` child element (design-ref/components/core/Icon.jsx). Failure: a standalone `<Icon name="paperclip" title="Attachment" />` outside a button no longer shows a native hover tooltip — the reference's form gives both the accessible name and the tooltip. Inside IconButton this is masked because `title` sits on the <button>.
- **[low]** ui/core/Button.tsx - Line 36: `var(--radius-pill, 999px)` references a custom property that exists in neither apps/web/src/styles/tokens.css nor any design-ref/tokens/*.css (grepped both — zero hits). It is a hard-coded 999px wearing a token's clothes. Not a font-size so it clears the letter of the no-raw-px rule, but it should be raised as a Foundation token request rather than a phantom var reference.
- **[low]** ui/core/Button.tsx - Line 28: `loading` is invented API — it does not appear in design-ref/components/core/Button.d.ts or Button.jsx. Harmless behaviourally (it sets aria-busy and blocks the click), but it is surface area the reference's UI kit and prompt.md do not document, so Phase 4 and any regenerated kit page will not know about it.
- **[low]** ui/core/IconButton.tsx - The whole file is a 7th component that design-ref core/ does not define (README lists exactly Button, Badge, Tag, StatusBadge+PriorityText, SyncDot, Icon). It also collides with design-ref/components/layout/RowActions.jsx, which composes raw `.btn .btn-sm .btn-icon` markup itself rather than delegating to Button. Under the README's 'Uniformity rule — one canonical implementation per element', Phase 4 will otherwise ship two icon-button implementations; someone has to decide which owns row actions.
- **[low]** ui/core/index.ts - Line 2: the comment 'APPEND to this file; never overwrite it (other Phase 2 groups add here too)' is factually wrong for this file. This is the core-only barrel; ui/forms, ui/data, ui/feedback, ui/layout and ui/navigation each already have their own index.ts. The comment will mislead the next agent into appending non-core exports here. Separately, no root apps/web/src/ui/index.ts exists and nobody has decided whether one should.

## data  (33)

- **[high]** ui/data/DataTable.tsx - L129-132 `showingCount()` re-implements, and drifts from, the canonical count-line copy that already exists in apps/web/src/ui/layout/ListFooter.tsx:53-62 (ported from design-ref/components/layout/ListFooter.jsx:7-11). Four concrete divergences: (a) the truncation string is `Showing first 1000 of 1240 sales orders — refine with search`, but the reference is `Showing first ' + limit + ' of ' + total + ' — refine with search'` with NO noun (ListFooter.jsx:9, ListFooter.tsx:59, README.md L69); (b) there is no zero case — showingCount(0,'sales orders') yields 'Showing all 0 sales orders' where the reference yields 'No sales orders' (ListFooter.tsx:54-55); (c) the client-filter case 'Showing 12 of 42 vendors' (ListFooter.tsx:60-61) is missing entirely — ANY list where total > shown is wrongly told to 'refine with search'; (d) no singular handling (ListFooter.tsx:62 `total === 1 ? noun : plural`). DataTable took a noun already pluralised by the caller, so 1 row reads 'Showing all 1 sales orders'.
- **[high]** ui/data/DataTable.tsx - L163-167 + L343-354: the count line renders at `var(--fs-xs)` (11px). The canonical count line is `var(--fs-sm)` (13px) — design-ref/components/layout/ListFooter.jsx line `fontSize: 'var(--fs-sm)'` and apps/web/src/ui/layout/ListFooter.tsx:72. The agent's note justifies --fs-xs from tables-list.html's 12px, but tables-list.html is a static mock; the canonical COMPONENT is ListFooter and it says 13px. Result: the same sentence renders at two different sizes depending on which component draws it.
- **[high]** ui/data/DataTable.tsx - L356-358: DataTable emits its own `💡 {hint}` div, byte-for-byte the same markup and styles as apps/web/src/ui/layout/ListFooter.tsx:105-116. UI_OVERHAUL_PLAN.md L?? (LIST pattern table, §d) and design-ref/README.md L46 both fix the list composition as `ListHeader → StatStrip|StatusPills → DataTable → ListFooter`, i.e. ListFooter owns the hint and the count line. A LIST screen that follows the template and passes `hint` to DataTable will render TWO hint lines. `countLine` (L108-109, L343-354) is the same duplication. Neither prop exists on design-ref/components/data/DataTable.d.ts — only `hint` does, and that predates ListFooter being extracted.
- **[high]** ui/data/StatStrip.tsx - Line 27 `import { Link } from '@tanstack/react-router'`. This is the ONLY router import in the entire apps/web/src/ui/ tree (verified: `grep -rn "@tanstack/react-router" apps/web/src/ui/` returns exactly this one line). The sibling layout group built `apps/web/src/ui/layout/link-slot.tsx` for precisely this case and states the rule in its header (lines 3-7): "ui/ must not import the router: a primitive has to render inside /__ui-kit with no route context. ... the caller passes `renderLink` - typically `(p) => <Link {...p} />` - and the slot stays router-free here." So the answer to review question 8 is NO for StatStrip: a `StatStrip` item with `to` set cannot render from props alone - it needs a RouterProvider, which breaks the Phase-2 `/__ui-kit` deliverable and any test render. It should take `renderLink` / use `LinkSlot`. Note the intent is right (audit D.7 forbids onClick+navigate for the Link case) - only the coupling is wrong.
- **[high]** ui/data/RelatedDocs.tsx - Lines 103-118 and 125-135 hand-roll an in-page tab strip while `apps/web/src/ui/navigation/TabStrip.tsx` already exists in this same overhaul and is documented there (line 1) as "THE one in-page tab look". README's headline uniformity rule says "in-page tabs -> TabStrip look" and "Every element type has exactly ONE component; screens compose them and never re-implement." Worse, the copy does not even match: RelatedDocs.tsx:108 uses `2px solid var(--blue)` where TabStrip.tsx:63 and design-ref/components/data/RelatedDocs.jsx:9 both use 3px; RelatedDocs.tsx:131 uses `var(--radius)` (6px) where TabStrip.tsx:45 and RelatedDocs.jsx:7 use `var(--radius2)` (8px); RelatedDocs.tsx:112 omits the `fontFamily: 'var(--bfont)'` TabStrip.tsx:67 sets. Result: two visibly different in-page tab looks now ship in ui/. The agent's note says it "followed the .jsx/README" - on the underline width and radius it followed neither.
- **[medium]** ui/data/DataTable.tsx - L278-293: `loading` and `empty`/`emptyText` re-implement apps/web/src/ui/feedback/PageState.tsx, which exists precisely for this and already has an `as="row"` placement taking `colSpan` (PageState.tsx:102-110) that renders `<tr><td colSpan className="empty-state">`. Two consequences: (1) the loading look now forks — PageState.tsx:30 is `'⟳ Loading…'`, DataTable draws shimmer bars, so a table's loading state differs from every other surface's; (2) DataTable's empty `<td>` at L290 carries NO className, whereas design-ref/components/data/DataTable.jsx puts `className="empty-state"` on the td itself and PageState.tsx:104 does the same. DataTable instead nests an EmptyState div inside a bare td.
- **[medium]** ui/data/EmptyState.tsx - L25 + L49: the `inline` prop (not present in design-ref/components/data/EmptyState.d.ts) sets `padding: var(--sp-3)` = 12px, and DataTable.tsx:291 uses it for the table's empty row. The reference puts 24px there — design-ref/components/data/DataTable.jsx renders the empty td with `style={{ padding: 24 }}`, and apps/web/src/ui/feedback/PageState.tsx:? sets `PAD.row = 'var(--sp-5)'` (24px). The empty row is therefore half the reference height.
- **[medium]** ui/data/DataTable.tsx - L288-293: the empty row's single `<td colSpan>` is `tbody td:first-child`, which apps/web/src/styles/innovic-theme.css:739-745 makes `position: sticky; left: 0` with a `box-shadow: 2px 0 4px -2px` whenever `.tbl-frozen` is on the wrap. So on a frozen table the empty message pins to the left edge and carries the pinned-column drop shadow instead of centring across the sheet. The group's notes claim frozen is now fully correct; the empty state is the case that is not.
- **[medium]** ui/data/DataTable.tsx - L199-210 vs L225/L238: when `rowActions` is given and `rowActionsWidth` is omitted (it is optional, L94, and has no default at L185), the appended Action column is pushed into `cols` with no `width`. `showColgroup` (L225) is still true because the caller's columns have widths, so a width-less `<col>` is emitted at L238. design-ref/components/data/DataTable.d.ts states widths 'should sum to 100 (fixed layout, no side-scroll)', so under `table-layout: fixed` the remaining width is 0 and the Action column collapses. Either default `rowActionsWidth` or require it.
- **[medium]** ui/data/SortHeader.tsx - L17 + L28-32: `SortDir` and `nextSort` are copied verbatim out of apps/web/src/components/shared/sortable-th.tsx:8-18 rather than re-exported from it. Both copies now exist and can diverge. design-ref/README.md L31 maps SortHeader ← sortable-th.tsx / sortable-head.tsx, i.e. it is meant to SUPERSEDE that file's logic, not fork it. Keeping sortable-th.tsx alive is defensible per the audit, but the pure helper should be imported once, not re-typed.
- **[medium]** ui/data/ItemBadge.tsx - Line 122: `boxShadow: tile ? '0 1px 3px rgba(20, 40, 70, 0.08)' : undefined` - this is the answer to review question 2: YES, there is a hard-coded rgba(). Two rules broken: (a) "Colours are tokens only (no raw hex)"; (b) README Visual Foundations - "Cards/panels: white, 1px --border, 8px radius, **no shadow**" and "Shadows: only floating layers - menus, toasts, modals". A 120px thumbnail tile is not a floating layer. It is copied verbatim from design-ref ItemBadge.jsx:9 and shared/item-badge.tsx:190, but the ban on raw colour is absolute and this was the rewrite. Either drop the shadow or add a `--shadow-*` token.
- **[medium]** ui/data/ItemBadge.tsx - Lines 31 and 131 import and render `Package` from `lucide-react` directly. `apps/web/src/ui/core/Icon.tsx` already exists in this overhaul, is documented as "the ONE control-icon source", and carries `'package'` in its IconName union and GLYPHS map. design-ref/components/data/ItemBadge.jsx does it the canonical way (line 2 imports `Icon`, line 11 renders `<Icon name="package" size={sc.icon} />`). So this is both a structural mismatch with the reference .jsx and a breach of the one-canonical-implementation rule inside ui/ itself.
- **[medium]** ui/data/Timeline.tsx - Lines 150 and 173 render the event's document code as `<span style={{ marginLeft: 'var(--sp-1)' }}>{e.code}</span>` - the `className="td-code"` and the blue colour are dropped. design-ref/components/data/Timeline.jsx has `<span className="td-code" style={{ marginLeft: 4, color: 'var(--blue)' }}>` in BOTH the compact (line 11) and regular (line 13) branches. `.td-code` (innovic-theme.css:687) is what makes a code mono/600. As written, a job-card number in the timeline renders in plain 13px Barlow body text - which is exactly the "item/doc code rendered quiet" failure mode the house rule forbids. In the compact branch it inherits nothing, in the regular branch it inherits the event colour and fontWeight 700 from the parent span (line 171), so it silently takes the status colour instead of the code colour.
- **[medium]** ui/data/MachineCard.tsx - Lines 61-64: the tile is ALWAYS a `<button type="button" aria-pressed={selected}>`, even when `onSelect` is undefined (line 78 acknowledges this by setting `cursor: 'default'`). A read-only machine tile on the Daily Report or Machine Loading page is then announced to a screen reader as a toggle button that is pressed or unpressed and does nothing when activated. The same agent got this right in StatStrip.tsx:112-140 (three element kinds; `<div>` when neither `to` nor `onClick`) and wrote the rationale in its header (lines 17-19, audit D.7: "neither -> <div> a plain total, not announced as a control"). MachineCard contradicts it.
- **[medium]** ui/data/MachineCard.tsx - Line 72 sets `minWidth: 'var(--field-md)'` = 144px while the component's own header comment (lines 4-5) and design-ref MachineCard.prompt.md instruct callers to lay tiles out with `grid-template-columns: repeat(auto-fill, minmax(140px, 1fr))`. In a grid that actually resolves a track to its 140px floor, a 144px-min tile overflows its own track by 4px. The reference used a raw `minWidth: 130` (MachineCard.jsx:3), which sits safely under 140. Replacing a raw px with the nearest token changed the number in the one direction that breaks the documented layout; either drop minWidth or tell callers to use minmax(144px, 1fr).
- **[medium]** ui/data/ItemBadge.tsx - Line 81: `const openable = hasImage && typeof onOpen === 'function'`, and lines 85-88, 128 gate role/tabIndex/keyboard/cursor on `openable`. Audit 02 §D.5 lists under MUST NOT CHANGE: "keyboard support (role=\"button\", tabIndex={0} only when `hasImage`, Enter/Space triggers open)" - gated on hasImage, not on hasImage AND a callback. In the live shared component (shared/item-badge.tsx:150-154) `open()` falls back to its own FilePreviewModal when `onOpen` is absent, so an image is ALWAYS clickable. In the pure version, any wrapper or screen that forgets to pass `onOpenImage` silently renders a dead, unfocusable picture - and because `onOpenImage` is optional, TypeScript will not catch it. Worth either making `onOpenImage` required when `src` is given, or documenting this as a wrapper invariant.
- **[medium]** ui/data/ItemBadge.tsx - Line 40 `BOX_PX = { row: 40, ... }` and line 318 `height: BOX_PX.row`. The running app takes this from `ITEM_IMAGE_SIZES` (packages/shared/src/schemas/item-image.ts:21 - `{ row: 48, card: 56, page: 96 }`), so today every list row thumbnail and every Thumbnail-column cell is 48px. The agent disclosed this, and design-ref ItemBadge.jsx:3 + audit §D.5 both do say 40 - but the audit is quoting the design reference, not the code. Net effect if swapped in as-is: every list's rows get 8px shorter and the 48px signed image is downscaled into a 40px box. Needs an explicit user decision, not an agent one; it is a one-line change either way.
- **[medium]** ui/data/StatStrip.tsx - Lines 125-135: the filter `<button>` cell is given no className, so it gets neither `dash-link` nor `dash-cell`. innovic-theme.css:466-472 defines `.dash-link.dash-cell:hover { background: var(--bg4) }` - so in one and the same strip, a navigation cell (line 117) lights up on hover and a filter cell next to it does not. design-ref StatStrip.jsx:7 applies `dash-link dash-cell` to whichever element is interactive. Related, same block: `color: 'inherit'` is applied only inside the `isLink` branch (line 78), whereas StatStrip.jsx:10 puts it on every cell - so the `<button>` falls back to the UA button colour for anything the children do not colour explicitly. (Both match the app's current shared/stat-strip.tsx, so this is a faithful port of an existing gap rather than a new bug - but the brief was to port against the design reference.)
- **[medium]** ui/data/Panel.tsx - Line 64 renders `<h2 className="panel-title">{hasHeader ? title : null}</h2>` - an EMPTY <h2> whenever `actions` is given without a `title`. innovic-theme.css:417-426 explains the class became a real heading because "screen readers navigate by heading, and a page whose every heading is a <span> is a page with no outline"; an empty heading pollutes that same outline. The comment on lines 62-63 gives the real requirement (keep the left flex slot), which an empty `<div>` or `justify-content: flex-end` satisfies without minting a blank heading.
- **[low]** ui/data/DataTable.tsx - L333-339: the `<tfoot>` slot is an invention with no design-ref precedent — `grep -rn tfoot design-ref/ apps/web/src/styles/innovic-theme.css` returns zero hits, and no guidelines/tables-*.html shows a '+ Line' row inside the table. The tfoot cell is outside `tbody tr:nth-child(odd)` so it gets none of the sheet's cream/white alternation and no dedicated styling at all.
- **[low]** ui/data/Skeleton.tsx - L7 `import './skeleton.css'` is the SECOND CSS import in the entire web app — apps/web/src/main.tsx:9 (`import './index.css'`) is the only other one, so this introduces a per-component CSS convention the repo does not have. Also `.ui-skeleton` / `.ui-skeleton-lines` use a `ui-` prefix that appears nowhere in innovic-theme.css's class vocabulary. The agent self-flagged the file; the prefix and the new import convention are the parts it did not flag.
- **[low]** ui/data/DataTable.tsx - L84: `maxHeight?: string` narrows design-ref/components/data/DataTable.d.ts's `maxHeight?: number | string`. Any migrating screen writing `maxHeight={400}` (the natural React form, and what the .d.ts sanctions) fails typecheck.
- **[low]** ui/data/Skeleton.tsx - L13 + L24: `height` defaults to `var(--fs-sm)` — a TYPOGRAPHY token used as a box dimension. 13px is not on the 2/4/8/12/16/24/32 spacing scale (design-ref/tokens/spacing.css). --sp-3 (12px) is the nearest sanctioned value.
- **[low]** ui/data/DataTable.tsx - L163-167: `UNDER_LINE_STYLE` is shared by the hint and the count line, so the count line inherits `padding: '0 var(--sp-1)'`. design-ref/components/layout/ListFooter.jsx applies that 4px gutter ONLY to the hint row; its count row has none. The count line therefore sits 4px further in from the right edge than the reference.
- **[low]** ui/data/DataTable.tsx - L303-310: `nowrap` and `ellipsis` are applied as inline styles on every td. House style is class-based (.td-code, .td-left, .td-num, .mono, .fw-700), and innovic-theme.css already scopes an ellipsis class (`.gs-ellipsis`, L1611-1613) rather than inlining. These are also redundant whenever `autoWidth` is set, since innovic-theme.css:785/788 already forces `white-space: nowrap` on every th/td of `.tbl-grid.tbl-auto`.
- **[low]** ui/data/DataTable.tsx - L148: `return v as ReactNode` casts an arbitrary field value to a renderable node. A Date or plain-object column value typechecks and then throws 'Objects are not valid as a React child' at runtime. design-ref/components/data/DataTable.jsx has the same hole, but it is untyped JSX; the cast here actively silences the compiler that would otherwise catch it.
- **[low]** ui/data/ItemBadge.tsx - Lines 236-238: the text block gets `onClick={onClick}` and `role={onClick ? 'button' : undefined}` but no `tabIndex` and no `onKeyDown` - a control that a keyboard user can neither reach nor activate. Contrast ItemImageBox in the same file (lines 85-110), which does it correctly. Carried over verbatim from shared/item-badge.tsx:296-298, so not a regression, but this was the rewrite and §D.5 is explicit about keyboard support on this component.
- **[low]** ui/data/ProgressBar.tsx - Lines 16 and 41 keep `height?: number` - a raw-px prop applied as an inline style. This is the exact shape the agent rejected for Panel (`bodyPadding`), writing in its own API note that "a raw px prop is exactly the thing the house rules ban". One of the two decisions is wrong; `.prog-wrap` already fixes 6px in innovic-theme.css:1383-1388, so the prop can most likely just go.
- **[low]** ui/data/Timeline.tsx - Answer to review question 2, spacing half: off-scale raw px survive in the rail geometry - lines 52-54 (`pad: 30`/`18`, `left: 13`/`5`, `dot: 20`/`10`), line 99 `paddingLeft: rail.pad`, line 108 `width: 2`, line 123 the computed dot offset, line 126 `top: 4`, line 168 `marginBottom: 2`. Same in StatStrip.tsx:67-68 (`flex: '1 1 140px'`, `minWidth: 120` - 120/140 are on neither the spacing scale nor the 64/104/144/224 field scale) and ItemBadge.tsx:208 (`nameMaxWidth` default 200) and :45-48 (icon 16/20/28/40). None is a font-size and all are taken from the reference .jsx, so this is defensible as geometry rather than spacing - flagging it because the question asked. No raw px font-size exists anywhere in the 8 files (verified by grep), and every font-size is on the 11/13/16/22/28 scale.
- **[low]** ui/data/RelatedDocs.tsx - Line 123 wraps the ruled `.innovic-table.tbl-grid tbl-compact` sheet (line 182) in a `<Panel>` with default body padding, i.e. inside `.panel-body`'s 12px. Panel.tsx:13-14, by the same agent, gives the rule: "`bodyPadding=\"none\"` a table fills the panel edge to edge - a ruled sheet inside 12px of padding reads as a box in a box." The tab strip does want the padding, so this needs a deliberate resolution (e.g. bodyPadding="none" plus a padded tab row), not the default.
- **[low]** ui/data/RelatedDocs.tsx - Line 196 puts `className="td-code"` on the `<td>` itself plus an inline `color: 'var(--blue)'`. design-ref RelatedDocs.jsx:14 uses `<td className="mono"><span className="td-code" style={{color:'var(--blue)'}}>` - a structural mismatch. The inline colour also duplicates innovic-theme.css:659-663 (`.innovic-table.tbl-grid .td-code { color: var(--blue) }`), so it is dead weight that will additionally fight a caller-supplied coloured node passed in as `code`.
- **[low]** ui/data/RelatedDocs.tsx - Lines 86-96 + 124 + 177: when every section is empty and there are no `extraTabs`, `shown` is [], `tabCount` is 0 and `active` is null - so the component renders a "🔗 Related Documents" Panel with a completely blank body and no message. `EmptyState` is imported (line 21) but is only reachable through the `showEmptySections` path (line 179), which is off by default. A document with no related paper yet should say so.
- **[low]** ui/data/Panel.tsx - API divergence worth carrying forward to the migration phase: `bodyPadding?: 'default' | 'none'` (line 24) vs design-ref Panel.d.ts `bodyPadding?: number | string`. The reference's own example in Panel.prompt.md (`<Panel title="Line items" bodyPadding={0}>`) will not compile. The reasoning is sound and I am not asking for it to change - but every copy-paste from design-ref will fail, so it belongs in the migration notes. Same class of thing: Timeline.tsx drops the reference's deprecated `variant?: 'rail' | 'compact'` back-compat prop (design-ref Timeline.d.ts) - harmless today, zero app callers.

## feedback  (14)

- **[high]** ui/feedback/Modal.tsx - Escape closes the whole dialog even when the Escape belongs to an open type-to-search dropdown. Modal.tsx:156-171 only skips when `e.defaultPrevented` is true, but components/shared/searchable-select.tsx:298-300 handles Escape with a bare `setOpen(false)` — no preventDefault, no stopPropagation — so the window listener fires and the form is lost. The app already solved this exact problem: lib/exit-guard.tsx:68 exports `escapeBelongsToAnOpenPicker(target)` and FIVE live modal screens call it before closing (lib/exit-guard.tsx:125, modules/incoming-qc/components/incoming-qc-inspect-modal.tsx:55, modules/qc-call-register/components/qc-call-inspect-modal.tsx:56, modules/machines/components/machine-group-tab.tsx:333, modules/raw-material/components/material-master-panel.tsx:409). audit/02 §D.1 lists 'Escape closes but keeps value' under MUST NOT CHANGE. The shared Modal — the one all five of those screens migrate onto in Phase 4 — drops the guard, so the first Escape aimed at a picker inside a form modal wipes the form.
- **[high]** ui/feedback/ConfirmDialog.tsx - Z-index regression against the file it is meant to replace. lib/exit-guard.tsx:188-205 (`ExitConfirmDialog`) defaults `zIndex = 600` unconditionally, and design-ref/components/feedback/ConfirmDialog.jsx:10 hard-codes `zIndex: 600` on the overlay. The new ConfirmDialog sits at 500 unless the caller passes `elevated` (ConfirmDialog.tsx:78 + Modal.tsx:256-257). A ConfirmDialog is BY DEFINITION raised over an already-open form, so 500 is the wrong default; the stated Phase-4 goal of re-pointing exit-guard 'without a copy change' would silently demote every exit guard from 600 to 500, and the one existing caller, ui/layout/RowActions.tsx:105-117, does not pass `elevated` either.
- **[medium]** ui/feedback/Modal.tsx - Uniformity-rule violation: the group hand-rolls `.btn` markup instead of composing ui/core/Button and ui/core/IconButton, which shipped in the same phase and which ui/layout/RowActions.tsx already consumes. Sites: Modal.tsx:231-239 (close ×), ConfirmDialog.tsx:129-142 (Cancel/Confirm), FilePreview.tsx:122-130 (Download), Banner.tsx:111-119 (dismiss), Toast.tsx:56-76 (dismiss). design-ref/components/feedback/Modal.prompt.md's own example composes `<Button variant="ghost">Cancel</Button>`, and ui/core/IconButton.tsx exists specifically to make the required `title` un-skippable. ui/core/Button.tsx also already has a `loading` prop that ConfirmDialog re-implements as `pendingLabel`.
- **[medium]** ui/feedback/Modal.tsx - Direct `lucide-react` imports instead of the canonical ui/core/Icon: Modal.tsx:29, Banner.tsx:17, Toast.tsx:24, FilePreview.tsx:24. design-ref/README.md 'Iconography' states control icons are Lucide *via* `Icon`, and ui/core/Icon.tsx already exports both glyphs used here ('x', 'download'). Self-declared by the author as a temporary cross-group dependency dodge; the dependency direction is already proven safe because ui/layout/RowActions.tsx imports ui/feedback. Six call sites.
- **[medium]** ui/feedback/Modal.tsx - Opening a modal puts focus on the header × instead of the first real field. Modal.tsx:146-147 focuses the first match of the FOCUSABLE selector inside the card, and the close button (Modal.tsx:231) is first in DOM order for every ordinary Modal — so 'New Purchase Request' opens with Close focused and a stray Enter dismisses the dialog. The app's own dialog does the opposite on purpose: lib/exit-guard.tsx:197-201 focuses Cancel with the comment 'Cancel takes focus, so a stray Enter keeps the form rather than losing it'. Needs to skip the close button or accept an `initialFocus`.
- **[medium]** ui/feedback/Modal.tsx - The Escape stack is ordered by effect-run order, not by nesting depth, so modal-over-modal Escape can close the wrong one. Modal.tsx:156-171 lists `onClose` in its deps; for the ordinary `onClose={() => setOpen(false)}` inline arrow that identity changes on every render, so any re-render of the OUTER modal runs its cleanup (line 168 splice) and re-pushes itself (line 158) to the END of `escapeStack`. Stack becomes [inner, outer]; one Escape then closes the outer dialog and orphans the inner one on screen. A depth counter or a ref-stable handler fixes it.
- **[medium]** ui/feedback/PageState.tsx - Wrong folder against the design authority itself, not just the audit: design-ref puts PageState at components/layout/PageState.jsx and README.md's component index lists it under **layout/** ('PageState (loading / error / empty / no-access)'). It also overlaps ui/data/EmptyState.tsx written by another group: PageState.tsx:128 re-renders the same `.empty-state` div with its own tone map instead of composing EmptyState, so the phase now ships two components that own the same class with DIFFERENT tone tokens (EmptyState.tsx:31-34 uses --sig-ok/--red, PageState.tsx:37-42 uses --red2/--amber2).
- **[medium]** ui/feedback/FilePreview.tsx - `filePreviewKind` (FilePreview.tsx:34-40) is a verbatim copy of the helper already living at components/shared/file-preview-modal.tsx:42-49 — same regex, same extension list, same order — rather than an import or a note that the original is to be deleted. Two copies of one classification rule will drift the first time someone adds .tiff or .heic to one of them.
- **[low]** ui/feedback/Modal.tsx - Modal.tsx:217 stops `mousedown` propagation on the card but not `click`. React portals bubble through the React tree, so a Modal rendered inside the app's standard clickable row/card (`<tr onClick={openDetail}>`) will navigate behind the dialog when a footer button is clicked. ui/layout/RowActions.tsx is safe only by accident, because its own wrapper at line 98 calls stopPropagation.
- **[low]** ui/feedback/Toast.tsx - Nested live regions: ToastStack sets `aria-live="polite"` on the container (Toast.tsx:117) while every child Toast carries `role="status"` (Toast.tsx:53), which is itself an implicit polite live region. Some screen readers announce each toast twice. Pick one level. Also Toast.tsx:153's comment says 'Most recent first' but `slice(-max)` at line 192 renders oldest-first with the newest at the bottom.
- **[low]** ui/feedback/Banner.tsx - Naming and colour choices now disagree ACROSS the phase, which is the opposite of the one-fact-one-name argument used to justify them. Banner.tsx:21 spells the tone `success` while ui/data/EmptyState.tsx:13 spells the same idea `ok` (design-ref Banner.d.ts also says `ok`). Same split on colour: Banner.tsx:32-35 and PageState.tsx:37-42 use --red2/--amber2/--green2/--blue2 (defensible per README 'solid = fills only'), while EmptyState.tsx:31-34 uses --red/--sig-ok, straight from the reference JSX. The parent has to rule once and make both groups match.
- **[low]** ui/feedback/PageState.tsx - Smaller deviations from the reference JSX, listed for a ruling: PageState `as="page"` pads var(--sp-6)=32px where design-ref/components/layout/PageState.jsx uses 40px and its .d.ts documents 'page (whole page, 40px)' — on-scale but visibly different. Banner.tsx:103 uses --sp-0 (2px) for the body's top margin, which README's rules block reserves for badges and compact/editable table cells (the reference JSX does the same, so the JSX and the rules block disagree). FilePreview.tsx:76-86 hand-rolls `.empty-state` for the unpreviewable case while the same component uses PageState for loading and error. FilePreview loses the reference's `aria-label="Close preview"` (it inherits Modal.tsx:235's generic 'Close'), and FilePreview.tsx:88 adds a ⟳ to 'Loading preview…' that the reference does not have. Toast.tsx:55-76 adds a dismiss × the reference has no prop for, styled with the only un-tokenized visual values in the group (border:0, opacity:0.8).
- **[low]** ui/feedback/ConfirmDialog.tsx - README says 'Required marker is ★ everywhere'. The typed-confirmation box (ConfirmDialog.tsx:185-205) is a required field — Confirm stays disabled until it matches — but its `.form-label` at line 187-189 carries no ★. No other required marker exists in this group, so this is the only site to check.
- **[info]** ui/feedback/Modal.tsx - Z-index audit result, for the record: nothing was invented. 500 and 9999 are the app's own values. The 600 at Modal.tsx:37 and 257 is NOT in the ladder your brief lists, but it IS design-ref's own value (design-ref/components/feedback/ConfirmDialog.jsx:10) and the app's (lib/exit-guard.tsx:191), so the ladder in the brief is what needs updating, not the code. Note the brief's ladder also omits 1000 (the searchable-select popover, design-ref tokens/components.css:186) and 20 (sticky ListHeader, design-ref/components/layout/ListHeader.jsx:4).

## forms  (23)

- **[medium]** ui/forms/SearchInput.tsx - CORRECTNESS BUG (line 69-77). The reset effect is keyed on `[value]` alone and compares against `reported.current`. The exact scenario the author's own notes claim to handle - "a cleared filter ... replaces the box contents and cancels any pending report" - does not work. Walk it: debounceMs=300, parent term is '', reported.current is ''. User types 'a' -> draft='a', timer pending, reported.current still ''. Parent's Clear-filters button calls setTerm('') - but value was ALREADY '' , so the prop does not change, the effect never re-runs, the box keeps showing 'a', and 300ms later the pending timer fires onChange('a') and re-applies the filter the user just cleared. Any parent-side reset to a value equal to the last reported term is silently swallowed. The effect needs to compare against the draft too, or the parent needs a reset signal.
- **[medium]** ui/forms/SearchInput.tsx - LAYOUT DEVIATION (line 105). design-ref/components/forms/SearchInput.jsx puts the width on the WRAPPER (`<div className="gs-wrap" style={{ width, ...style }}>`, default `var(--field-lg)`). The implementation puts width only on the <input> (via .fw-* or inline 100%) and leaves .gs-wrap with no width at all. .gs-wrap in innovic-theme.css:1525 is only `position:relative; flex-shrink:0` - no width - so outside a flex row the wrapper div stretches to 100% of its parent while the input stays 224px, leaving an invisible full-width block that pushes siblings. That is precisely what the reference's wrapper width prevents. Also, .gs-wrap's `flex-shrink:0` combined with the `width:'100%'` this file sets for width="full" makes the box unable to shrink inside a flex toolbar, so it can overflow.
- **[medium]** ui/forms/FormField.tsx - API GAP (props block line 30-44; class assembly line 57). design-ref/components/forms/FormField.jsx carries a `span?: 'full' | 2` legacy prop that emits `.form-full` / `.form-span-2`. Those two classes exist in the live theme (innovic-theme.css:1098 .form-span-2, :1103 .form-full) but NO prop in this implementation can emit either one. Combined with FormGrid still advertising the legacy `cols` escape, the legacy path is now half-removed and actively broken: FormField ALWAYS emits an f-* class, and `.f-md { grid-column: span 4 }` (innovic-theme.css:1058) inside `.form-grid-4 { grid-template-columns: repeat(4,1fr) }` makes every field occupy the whole row - one field per row instead of four. So `<FormGrid cols={4}>` + `<FormField>` renders wrong, and there is no `span` prop left to correct it. Either drop `cols` from FormGrid or restore `span`.
- **[medium]** ui/forms/DocNumberInput.tsx - Line 112: className="animate-spin" is a Tailwind utility class. The brief says "Do NOT introduce Tailwind utilities" in ui/. It works today only because Tailwind is still installed app-wide; it is the one Tailwind class in the whole five-file set. The spin should be a keyframe in innovic-theme.css (design-ref renders the checking state as the plain text glyph '…' at font-size var(--fs-sm), no spinner at all).
- **[medium]** ui/forms/DocNumberInput.tsx - Line 66: `id = 'doc-number'` is a FIXED default id, not useId(). Two or more DocNumberInputView on one page all get id="doc-number" -> duplicate DOM ids, and every <label htmlFor> focuses the first box. This directly breaks the component's own stated purpose ("what the kit page renders in all five states" = five of them side by side). LineItemPicker.tsx:94 and FileField.tsx:85 both use useId correctly; this one is the odd one out. The live component avoids it with `id ?? `docno-${type}``.
- **[medium]** ui/forms/DocNumberInput.tsx - Lines 126-133 (hintFor): returns undefined when state='idle' AND the box is non-empty, so the line under the field disappears entirely and the field's height jumps as you type. design-ref/components/forms/DocNumberInput.jsx always falls through to the auto-fill hint for idle, and the live components/shared/doc-number-input.tsx:132-138 always falls through to "✓ Available". Neither ever renders nothing.
- **[medium]** ui/forms/DocNumberInput.tsx - Line 26: imports Check, Loader2, X straight from 'lucide-react' with raw size={14}/size={15}, bypassing the sibling primitive apps/web/src/ui/core/Icon.tsx which exists for exactly this and already carries 'check', 'x' and 'loader-2' with a 14px default. The notes claim the file composes sibling primitives "rather than re-hand-rolling"; that was done for FormField but not for Icon.
- **[medium]** ui/forms/FileField.tsx - Lines 172-178: the image variant's Choose/Change trigger is a <label className="btn btn-ghost btn-sm">, not a <button>. design-ref/components/forms/FileField.jsx and the live modules/items/components/item-image-field.tsx:60-67 both use a real <button type="button"> that .click()s the hidden input. A <label> is not keyboard-focusable, so the control cannot be reached by Tab, and `.btn:disabled` never applies so it shows no disabled state while busy (only the manual opacity 0.6 does).
- **[medium]** ui/forms/FileField.tsx - Lines 152-162 vs 172-178: FormField already renders the field's own <label className="form-label" htmlFor={inputId}>, and the image variant then adds a SECOND label with the same htmlFor. Clicking the plain title text "Product image" now opens the OS file dialog. design-ref's title label carries no htmlFor.
- **[medium]** ui/forms/SearchableSelect.tsx - Checklist item 1 fails for this component: the thing exported from ui/forms does not render design-ref's markup at all. It passes straight through to components/shared/searchable-select.tsx, which draws the box with the shadcn <Input> and paints the portaled list with Tailwind utilities (line 346 'bg-popover text-popover-foreground border-input shadow-md', line 349/351 'px-3 py-2 text-sm text-muted-foreground', line 366 'bg-accent text-accent-foreground'). Option rows are 12px/8px padded vs .ss-opt's 4px/8px, so the kit's most-used picker will look visibly unlike the reference. The agent flagged this and is correct that it cannot be fixed from a wrapper (the classNames are internal and the list is portaled to <body>), but the deviation is real and .ss-list/.ss-opt/.ss-muted in innovic-theme.css are dead CSS until Phase 4.
- **[low]** ui/forms/SearchInput.tsx - STALE DEVIATION (line 16). The author's note #2 justifies `import { Search } from 'lucide-react'` with "ui/core did not exist when this was written". It exists now: apps/web/src/ui/forms/../core/Icon.tsx is present, is a typed lucide wrapper, and already declares the 'search' name (IconName union). design-ref/components/forms/SearchInput.jsx imports `{ Icon } from '../core/Icon.jsx'` and renders `<Icon name="search" size={14} />`. The stated one-line swap is now due; leaving it means two icon entry points in the same primitive library.
- **[low]** ui/forms/SearchInput.tsx - DUPLICATED LOGIC (lines 63-100). apps/web/src/lib/use-debounce.ts already exists and its header comment literally says "this is the shared one used by useDocNumber + future forms". This file hand-rolls its own setTimeout/useRef debounce instead. The hook's value-in/value-out shape does not cover the draft-ahead-of-value reset semantics, so a straight swap is not possible - but the repo now carries three debounce implementations (use-debounce.ts, searchable-select.tsx's inline one, and this). Either extend the shared hook or say in the file why it cannot be used.
- **[low]** ui/forms/SearchInput.tsx - UNUSABLE PROP VALUES (line 114). `width` accepts 'xs' and 'sm', which map to .fw-xs (--field-xs = 64px) and .fw-sm (104px). But .innovic-input.gs-input (innovic-theme.css:1537) hard-sets `padding: 0 var(--sp-2) 0 28px` to clear the magnifier, and .fw-* carries `!important` so it wins the width fight. A 64px search box leaves ~28px of typing room. The design-ref d.ts documents only the --field-lg default and offers no size ladder. Either drop xs/sm from the union or document them as unsupported.
- **[low]** ui/forms/Select.tsx - SPEC ADDITION, INCOMPLETE (line 37). `placeholder` renders `<option value="">{placeholder}</option>` with neither `disabled` nor `hidden`. On a required select the user can re-pick the empty option after choosing a real one, which native validation will then reject at submit rather than at pick time. design-ref/components/forms/Select.d.ts has no `placeholder` prop at all, so this is an un-speced addition; if it stays it should be `disabled hidden` like every other placeholder option convention.
- **[low]** ui/forms/DocNumberInput.tsx - Line 80: default error copy 'Duplicate — this number already exists' matches neither design-ref ('Already used — pick another number') nor any string docNumberError/useDocNumber produces. It is invented wording inside a file whose header claims "the exact §D.2 message strings".
- **[low]** ui/forms/FileField.tsx - Line 93: `e.target.value = ''` runs for ALL three variants. For variant="drawing" the native <input type="file" className="innovic-input"> IS the visible control, so it snaps back to "No file chosen" immediately after a successful pick. design-ref's drawing branch deliberately does not clear (only the hidden-input branches do), and the live drawing-upload-field.tsx does not clear either. The stated rationale ("re-choosing the same file still fires") only applies to the hidden inputs.
- **[low]** ui/forms/FileField.tsx - Line 67: DEFAULT_ACCEPT = 'image/*,.pdf' is applied to the image variant too, so by default a PDF passes a field whose own hint reads "3D render — JPG, PNG or WebP, up to 5 MB". The live item-image-field.tsx uses ITEM_IMAGE_MIME_TYPES. (design-ref carries the same sloppy default, so this is inherited, not invented.)
- **[low]** ui/forms/SearchableSelect.tsx - Line 102: `style` is applied to a bare wrapper <div> that has no position:relative. design-ref applies style to the POSITIONED root (`style={{ position:'relative', ...style }}`), so a caller passing top/left/transform gets a different result here than in the reference.
- **[low]** ui/forms/LineItemPicker.tsx - Lines 149-156: on a name edit the payload is `{ itemId: matchedItem?.id ?? null, matched: false }`. When the code matches the master this reports a non-null itemId alongside matched:false — the two fields of the new superset contract contradict each other. Mostly unreachable (the name input is readOnly while matched), but it is wrong in the contract that Phase 4 is supposed to swap in mechanically.
- **[low]** ui/forms/LineItemPicker.tsx - Reuse: all three new files hand-roll <input className={cx('innovic-input', …)}> instead of composing the sibling primitive apps/web/src/ui/forms/Input.tsx, which already maps state -> .is-ok / .is-bad / .is-derived. See LineItemPicker.tsx:115-123 and 142-157, DocNumberInput.tsx:84-99, FileField.tsx:198-205. FormField was composed; Input was not.
- **[low]** ui/forms/index.ts - Line 36 exports both DocNumberInput (the LIVE, hook-backed component that requires `type` and fires a duplicate-check request) and DocNumberInputView under one barrel. So checklist item 8 is only half true: anyone importing the obvious name `DocNumberInput` from the kit gets something that cannot render without a network call. Separately, the /__ui-kit page the notes repeatedly cite does not exist anywhere in the worktree (grep for "__ui-kit" returns nothing), so "renders in all five states with no fetch" is unproven by any consumer.
- **[low]** ui/forms/SearchableSelect.tsx - Factual error in the agent's handoff notes (point 7): it claims the forms group kebab-cases 'searchable-select.tsx'. The file is SearchableSelect.tsx - PascalCase. Only non-component helper modules are kebab (class-names.ts, link-slot.tsx), which is a consistent convention. There is no naming conflict to resolve before Phase 3 and no rename is needed.
- **[info]** ui/forms/SearchInput.tsx - STRUCTURAL DIFFERENCE (line 108), informational. design-ref renders `<span className="gs-icon"><Icon name="search" size={14} /></span>` - the class on a wrapping span. This file puts className="gs-icon" directly on the <Search> svg. It renders identically (.gs-icon is position:absolute + pointer-events:none, innovic-theme.css:1529) and it is byte-for-byte what components/shared/global-search.tsx:99-113 does today, which is what audit/02-element-inventory.md line 41 asked for. Flagging only so the parent knows the reference JSX and the app markup disagree here and the app won.

## layout  (13)

- **[high]** ui/layout/link-slot.tsx - L37-48 + L74: BUTTON_RESET is spread BEFORE the caller's style ({...BUTTON_RESET, ...style}), so every key the caller does not restate survives and, being inline, beats the class CSS. QuickLinks (WorkList.tsx:328-343) passes className="btn btn-sm" and a style that only sets width/background/color/border/fontSize -- so padding:0, display:'block', font:'inherit' and textAlign:'left' from BUTTON_RESET win over .btn's `display:inline-flex; padding:0 var(--sp-3)` and .btn-sm's `padding:0 var(--sp-2)` (innovic-theme.css:962-1013). Result: a Quick Access chip with ZERO horizontal padding and no flex centring inside its 24px height. This is the exact path the design reference uses (design-ref WorkList.jsx QuickLinks takes `onClick`, not `to`), and audit/02 line 107 says QuickLinks must be 'ported as-is'. Compounded by the next finding.
- **[medium]** ui/layout/WorkList.tsx - L334-340: QuickLinks dropped the reference's explicit chip padding. design-ref/components/layout/WorkList.jsx QuickLinks sets `padding: '4px 8px'` on the chip (and today's apps/web/src/modules/dashboard/components/quick-links.tsx:20 uses '5px 10px'). The rewrite removed it and added `width:'auto'` instead, so chip height/padding now come from .btn-sm (24px tall, 0 8px). Combined with the BUTTON_RESET bug above, the onClick-only path gets no padding at all and the renderLink path gets a taller chip than the reference.
- **[medium]** ui/layout/WorkList.tsx - L275 and L224: the two dashboard radii deviate from design-ref in OPPOSITE directions, so neither is a consistent 'snap to nearest token' policy. StatRow surface uses borderRadius 'var(--radius2)' = 8px where design-ref WorkList.jsx StatRow uses `borderRadius: 6` (= --radius). AttentionList surface uses 'var(--radius)' = 6px where design-ref uses `borderRadius: 4` (= --radius-sm). audit/01-foundation-delta.md:164-166 confirms --radius-sm/--radius/--radius2 = 4/6/8 in both ref and app, so the correct tokens were available and were not used.
- **[medium]** ui/layout/WorkList.tsx - L123: the age chip changed from a fixed `width: 36` (design-ref WorkList.jsx, and the live my-work-panel.tsx:43) to `minWidth: 'var(--sp-6)'` = 32px. Two regressions in one edit: (a) 36px -> 32px, and (b) fixed width -> min width, so a row with '12d' or '120d' grows and the severity-coloured action buttons stop forming a straight right-hand column across the list -- the visual rhythm the fixed width existed to create. It also uses a SPACING token as a WIDTH, which tokens.css:224-233 and design-ref README line 39 ('no inline pixel widths', use the --field-*/.fw-* width tokens) do not sanction.
- **[medium]** ui/layout/DocCard.tsx - L196-208: LinesPanel's 'Open full detail →' was turned into `className="btn btn-ghost btn-sm"`. design-ref/components/layout/DocCard.jsx renders a bare inline `<a>` with only fontSize/color/cursor -- a text link. .btn/.btn-ghost/.btn-sm give it height:24px, a border box and padding 0 8px (innovic-theme.css:962, 1008, 1015-1021), so it now reads as a chrome-bearing button sitting next to the mono '▸ LINE ITEMS — CODE' band label instead of a quiet link. Using a real <button> for the a11y is right; styling it as a ghost button is the deviation (there is no .btn-link in innovic-theme.css to fall back on).
- **[medium]** ui/layout/link-slot.tsx - L59-68: `to` silently wins over `onClick` and there is no onClick on the <a> fallback. AttentionItem (WorkList.tsx:178-179) and StatRowProps (WorkList.tsx:253-255) both expose `onClick` AND `to`. A Phase-4 port that keeps its existing `onClick={() => navigate(...)}` while also adding `to` gets the onClick dropped with no warning; and if it forgets `renderLink`, the row becomes a plain `<a href>` that does a FULL PAGE RELOAD of the SPA instead of a router navigation. The precedence is documented in the header comment but nothing in the types or at runtime stops the mistake.
- **[medium]** ui/layout/WorkList.tsx - L128: `className="sr-only"` is a Tailwind utility, which the house-style rule for this phase forbids in ui/. It resolves only because index.css still emits `@tailwind utilities`; nothing in styles/tokens.css or styles/innovic-theme.css defines .sr-only, so if the foundation work ever drops the utilities layer this becomes visible text. Separately, the announced string is the raw enum value -- a screen reader says 'warn, 5d' / 'info, ·', which is not English. design-ref's WorkList.jsx has no sr-only at all; this was carried over verbatim from my-work-panel.tsx:47.
- **[medium]** ui/layout/PageHeader.tsx - L131-193 is a near-verbatim second copy of ListHeader.tsx:68-133: same sticky/non-sticky style branch (position:sticky, top:0, zIndex:20, background var(--bg), paddingBottom/marginBottom var(--sp-2), 1px var(--border) bottom), same flex title row with `marginBottom: children ? var(--sp-2) : 0`, same .section-hdr + icon prefix, same .text3 subtitle at var(--fs-sm)/var(--sp-0), same children band. Two copies of the same band across ~97 list screens and every form screen will drift -- the exact failure the ListHeader consolidation exists to end. One component with a `variant`, or PageHeader composing ListHeader's band, avoids it.
- **[low]** ui/layout/RowActions.tsx - L68-74: icon mode dropped the reference's explicit `padding: '4px 4px'` (design-ref RowActions.jsx) and relies on `.btn-icon { padding: 0 var(--sp-2) }` (innovic-theme.css:1015-1021) = 0 8px. Icon buttons are therefore 8px-padded and 24px tall rather than the reference's tight 4px box, so the Action column of all 45 migration sites gets wider than spec. Defensible as house style, but it is an unlisted deviation -- it is not in the agent's own deviation list.
- **[low]** ui/layout/ListFooter.tsx - L58: guard changed from design-ref's truthy `limit && total > limit` to `limit != null && total > limit`. A caller passing `limit={0}` (a plausible 'no cap' sentinel) now renders 'Showing first 0 of 42 — refine with search' instead of falling through to 'Showing all 42 …'.
- **[low]** ui/layout/DocCard.tsx - L173: LinesPanel adds `onClick={(e) => e.stopPropagation()}` on its outer band. design-ref/components/layout/DocCard.jsx LinesPanel has no such handler. Inside DocCard it is a no-op (the expanded band has no ancestor click handler -- only the identity and metric bands carry onToggle), but it silently changes LinesPanel's public contract: any Phase-4 screen that places a LinesPanel inside its own clickable surface will find that surface dead.
- **[low]** ui/layout/WorkList.tsx - L57: `padding: 0` added to the .panel wrapper; design-ref WorkList.jsx uses only `marginBottom: 12`. .panel (innovic-theme.css:398-407) declares no padding, so this is a dead declaration. L156: `className="wl-footer"` is not defined in styles/tokens.css, styles/innovic-theme.css or index.css -- a dead class, carried over from my-work-panel.tsx:98 where it is equally dead. Neither is in the reference.
- **[low]** ui/layout/ListHeader.tsx - L101 (and PageHeader.tsx:174, DetailHeader.tsx:78) use `marginTop: 'var(--sp-0)'`. These are faithful conversions of the reference's literal `2`, but tokens.css:224-227 scopes --sp-0 (2px) to 'badge/tag vertical, compact & editable table cells only'. So the token's own documented contract is broken in three places. Ref-driven rather than invented, but the tokens file is the stated source of truth and it disagrees with itself here -- worth a decision before ~97 screens inherit it.

## navigation  (9)

- **[high]** ui/navigation/FilterBar.tsx - Line 91: `value={f.value ?? ''}` makes every select controlled with a value that matches no option for the plain-string options shape design-ref documents (FilterBar.prompt.md: options: ['All Status','To Do','In Progress']). selectedIndex becomes -1 and the dropdown renders BLANK. design-ref passes `value={f.value}` (undefined -> uncontrolled -> shows option 0). This breaks the component's own stated house rule that an off filter still names itself, and it breaks it exactly on /__ui-kit where no value is passed. It only works for the Task Board shape where the first option literally has value="". The JSDoc on FilterDef.value (line 26) claiming '"" is the first, filter-naming option' is wrong for half the accepted union.
- **[medium]** ui/navigation/Breadcrumbs.tsx - Line 72: the <a> fallback is `<a href={c.to} onClick={() => onNavigate?.(c)}>` with no e.preventDefault(). A caller that supplies onNavigate (the router-free path the prop exists for) gets its handler AND the browser's default full-page reload of href. design-ref's <a> has no href at all, only the handler.
- **[medium]** ui/navigation/PageTabs.tsx - Lines 97-102: the renderLink branch omits aria-current, and RenderLinkArgs (ui/layout/link-slot.tsx:14-20) has no field to carry it. The <a> branch (line 112) and the div branch (line 125) both set it. Phase 3 wires renderLink, so the agent's claim of 'aria-current="page" on the active one' is false on the only path production will use.
- **[medium]** ui/navigation/PageTabs.tsx - Lines 97-117: onSelect is reachable only in the third branch (no `to`). design-ref's PageTabs calls onSelect on every tab click. The shell always supplies `to`, so onSelect is a silently dead prop there, with no warning in its doc comment (line 47) and no type-level signal.
- **[medium]** ui/navigation/TabStrip.tsx - Line 80: undisclosed override on the count badge - `style={{ borderRadius: 'var(--radius2)', padding: 'var(--sp-0) var(--sp-2)' }}`. innovic-theme.css:900-906 already sets .badge to exactly that padding (dead override) and to border-radius var(--radius-sm)=4px. Forcing --radius2=8px applies the panel/card radius to a badge, contradicting spacing.css:20 ('--radius-sm: badges, tags, chips') and README 'screens never override a component's size'. design-ref's 10px is a legacy pill value; the on-token port is to delete the override, not substitute the nearest-looking token. Not listed among the agent's two declared deviations.
- **[medium]** ui/navigation/StatusPills.tsx - Wrong folder. audit/02-element-inventory.md:337 and design-ref/components/layout/StatusPills.jsx both place StatusPills (+ViewToggle) under ui/layout/. It sits in ui/navigation/ and is exported from ui/navigation/index.ts:23, while ui/layout/index.ts does not export it and ui/layout/ListHeader.tsx:3,11,46 documents StatusPills as the thing filling its children slot. Anyone following the approved plan imports from ui/layout and either fails or builds a duplicate. The agent flagged this itself and shipped it anyway.
- **[low]** ui/navigation/PageTabs.tsx - Lines 82-93: the .pgtab-close x is an icon-only button with aria-label but no title, violating design-ref/README.md:98 ('Icon-only buttons always carry a title'). The live open-tabs-bar.tsx has the same gap, so it is inherited rather than new, but the README rules block is the stated authority.
- **[low]** ui/navigation/Breadcrumbs.tsx - Line 57 (and PageTabs.tsx:69) hard-code id="breadcrumbs" / id="pagetabs". The component therefore cannot be rendered twice on one page, which /__ui-kit must do to show 'Breadcrumbs (any depth)'. It also collides with the still-live components/shared/breadcrumbs.tsx:63 and open-tabs-bar.tsx:75 while both exist. The theme's selectors are dual (innovic-theme.css:265,288), so the class alone suffices and the id buys nothing.
- **[low]** ui/navigation/PageTabs.tsx - Lines 107-116: the router-free branch nests a <button> inside <a href>, which is invalid HTML (nested interactive content). Preserved from the live <Link> version; design-ref avoids it by using a <div>. Worth knowing before an a11y audit.
