name: Deploy

# API deploy is handled by Railway's GitHub integration (ADR-010), NOT this workflow.
# Railway watches `main` and runs the Dockerfile at apps/api/Dockerfile.
# This workflow exists for the web deploy to Cloudflare Pages, kept disabled
# until that's wired (separate task; not part of T-011).

on:
  workflow_dispatch:

jobs:
  deploy-web:
    if: false # disabled until Cloudflare Pages is wired
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "TODO: deploy frontend to Cloudflare Pages"
