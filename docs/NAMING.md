# NAMING.md — one name, one meaning

> Audit date: 2026-09-23. Triggered by the `CPO` → `POL` fix, which found one concept
> under four labels and one label covering two concepts.
>
> **The rule:** every fact below has exactly ONE screen label and ONE field name.
> A name in the "never" column is a defect, not a synonym. Re-reading this file must
> give the same answer every time — that is the point of it.
>
> Nothing is renamed by this document. Section C is the decision list.

---

## A. Canonical dictionary — facts

| Fact                                                               | Screen label                         | Field name                                     | Source of truth                                                                               | Never call it                                                                                         |
| ------------------------------------------------------------------ | ------------------------------------ | ---------------------------------------------- | --------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| Item's code                                                        | `Item Code`                          | `itemCode`                                     | `items.code`                                                                                  | `Part No`, `Part / Item No.`, `partNoText`, `childItemCode`                                           |
| Item's code, snapshot                                              | — (fallback only)                    | `itemCodeText`                                 | the line's own column                                                                         | reading the snapshot when the live join exists                                                        |
| Item's name                                                        | `Item Name`                          | `itemName`                                     | `items.name`                                                                                  | `Part Name`, `Part`, `Item Made`, `partName`, `itemNameText`, `partText`                              |
| **Customer's drawing revision**                                    | never alone — only inside `CODE/REV` | `itemRevision`                                 | `sales_order_lines.revision`, else `job_work_order_lines.revision`                            | bare `Rev`, `soLineRevision`, `jwLineRevision`, `currentRevision`, `lineRevisionText`                 |
| Item master's own revision                                         | `Item Master Rev` (or not shown)     | `revision` on the item                         | `items.revision`                                                                              | anything labelled just `Rev`                                                                          |
| Route card's version                                               | `Route Card Rev`                     | `currentRevision`                              | `route_cards.current_revision`                                                                | `Rev`, `Revision`                                                                                     |
| BOM's version                                                      | `BOM Rev`                            | `revision` (integer)                           | `bom_masters.revision`                                                                        | `Rev`, `Revision`                                                                                     |
| Design's version                                                   | `Design Rev`                         | `revision` (integer)                           | `design_tracker.revision`                                                                     | `Rev`                                                                                                 |
| **Customer's PO line number**                                      | `POL`                                | `clientPoLineNo`                               | `sales_order_lines.client_po_line_no`                                                         | `CPO`, `CPO Ln`, `Client PO Ln`, `PO Line#`, and never our `lineNo`                                   |
| Customer's PO number                                               | `Client PO No.`                      | `clientPoNo`                                   | `sales_orders.client_po_no`                                                                   | `CPO`, `Client PO`                                                                                    |
| Our line number on the order                                       | `Ln`                                 | `lineNo`                                       | `sales_order_lines.line_no`                                                                   | `#`, `Line`, `Line #`, `SO Line`, `SO / WO Line`, `POL`                                               |
| Sales-order number                                                 | `SO No.`                             | `soCode`                                       | `sales_orders.code`                                                                           | `SO #`, `Order No`, `SO/WO No.`, `SO ref`, `SO code text`, `SO/JC Ref`                                |
| Job-card number                                                    | `JC No.`                             | `jcCode`                                       | `job_cards.code`                                                                              | `Job Card No.`, `Job card`, `JC Source`, `Source JC`, `Linked JC`, `jobCardCode`                      |
| Operation sequence                                                 | `Op`                                 | `opSeq`                                        | `jc_ops.op_seq`                                                                               | `Sr No`, `Op Sr No`                                                                                   |
| Machine an op is planned on                                        | `Planned Machine`                    | `machineCode` (the op's)                       | `jc_ops.machine_id` → `machines.code`                                                         | `Machine`, the stacked `Machine (Planned / Actual)`                                                   |
| Machine an op actually ran on                                      | `Actual Machine`                     | resolved (`resolveActualMachine`)              | open session's machine, else `v_op_machine_output`, else the plan                             | `Machine`, the stacked `Machine (Planned / Actual)`                                                   |
| Row counter in a list                                              | `Sr No`                              | — (not stored)                                 | —                                                                                             | anything that is a real field                                                                         |
| Qty ordered on an order line                                       | `Order Qty`                          | `orderQty`                                     | `<doc>_lines.order_qty`                                                                       | `Ordered`, `ORDER`, `Qty (Plan)` — but see the three rows below, which are DIFFERENT quantities       |
| Qty on a purchase-side line                                        | `Qty`                                | `qty`                                          | `purchase_order_lines.qty` etc.                                                               | `orderQty`                                                                                            |
| Qty of an order already put on job cards                           | `JC Qty`                             | `jcQty`                                        | computed                                                                                      | `Order Qty` — it sits in the same table as the real one                                               |
| Qty a plan is for                                                  | `Plan Qty`                           | `planQty`                                      | `plans.plan_qty`                                                                              | `Order Qty`, `Planned Qty`                                                                            |
| Qty a purchase request asks for                                    | `PR Qty`                             | `qty` on the PR                                | `purchase_request_lines.qty`                                                                  | bare `Qty` where an order qty is also shown                                                           |
| Qty produced at an operation                                       | `Completed`                          | `completedQty`                                 | `jc_ops.completed_qty`                                                                        | `Done`, `DONE`, `Produced`, `Qty Made Here`, bare `Qty`                                               |
| Qty passed inspection                                              | `Accepted`                           | `qcAcceptedQty`                                | `goods_receipt_note_lines.qc_accepted_qty`                                                    | `Acc`, `OK`, `Passed`, `acceptedQty`, `okQty`                                                         |
| Qty failed inspection                                              | `Rejected`                           | `qcRejectedQty`                                | `goods_receipt_note_lines.qc_rejected_qty`                                                    | `Rej`, `Reject`, `Rejected (NC)`, `QC Rejected`                                                       |
| Qty still owed                                                     | `Pending`                            | `pendingQty`                                   | computed                                                                                      | `Balance`, `Remaining`, `Outstanding`, `Shortfall`                                                    |
| Pieces an order put into stock                                     | `Credited Qty`                       | `creditedQty`                                  | `production_orders.credited_qty`                                                              | `Produced`, `Made`                                                                                    |
| Pieces an order wrote off (scrap)                                  | `Lost Qty`                           | `lostQty`                                      | `production_orders.lost_qty`                                                                  | `Scrap Qty`, `Process Loss`, `Rejected` (that is the QC fact)                                         |
| Qty of a plan already on orders                                    | `Covered`                            | `coveredQty`                                   | computed — `lib/plan-order-coverage.ts` (ADR-184 / ADR-185)                                   | `Ordered`, `In Production`                                                                            |
| Qty free to use now                                                | `Available`                          | `availableQty`                                 | computed — state the grain in the field's comment                                             | `Avail`, `available`                                                                                  |
| Date a thing is wanted by                                          | `Due Date`                           | `dueDate`                                      | `<doc>.due_date`                                                                              | `Due`, `DUE`, `targetDate`, `requiredDate`                                                            |
| Any other date                                                     | `<Event> Date`, e.g. `GRN Date`      | `<event>Date`                                  | that document's own column                                                                    | bare `Date`, bare `date`                                                                              |
| Person who did it (id)                                             | —                                    | `createdBy` / `updatedBy`                      | `<table>.created_by`                                                                          | `userId`, `enteredBy`                                                                                 |
| Person's name (snapshot)                                           | `<Role>`, e.g. `Operator`            | `<role>Name`, e.g. `operatorName`              | `<table>.<role>_name`                                                                         | bare `By`, `…Text` for a person                                                                       |
| The buyer                                                          | `Customer`                           | `clientId` / `clientName`                      | `clients`                                                                                     | `Client`, `Party`, `Bill to / Client`                                                                 |
| The seller to us                                                   | `Vendor`                             | `vendorId` / `vendorName`                      | `vendors`                                                                                     | `Supplier`, `Vendor / Supplier`, `Party`                                                              |
| Days a customer is allowed to pay an invoice in (ADR-188)          | `Payment Days`                       | `paymentDays`                                  | `clients.payment_days`                                                                        | `Credit Days`, `Payment Terms` (that is the invoice's own, below), `paymentTermsDays` on the customer |
| Days one invoice allows for payment                                | `Payment Terms (days)`               | `paymentTermsDays`                             | `invoices.payment_terms_days` — defaulted from the customer's `Payment Days`                  | `Payment Days` (that is the customer's default), `creditDays`                                         |
| Design Tracker a work-log row was logged from (ADR-188)            | Tracker (badge)                      | `designTrackerId`                              | `design_work_log.design_tracker_id`                                                           | `trackerId`, `dsnId`                                                                                  |
| Qty of an SO line / a customer dispatch already invoiced (ADR-189) | `Billed`                             | `billedQty`                                    | Σ `invoice_lines.qty` per SO line; per dispatch derived oldest-first (no invoice→dispatch FK) | `invoicedQty`, `Invoiced`                                                                             |
| How far a customer dispatch is invoiced (ADR-189)                  | `Billed` (none / partial / full)     | `billedStatus`                                 | computed from `billedQty` vs the dispatch's qty                                               | `invoiceStatus` — that name is the invoice's own payment status (unpaid / partial / paid)             |
| The page a record opens on (ADR-189)                               | — (row link)                         | `navPage`                                      | `docNavPage()` in @innovic/shared; tasks store it in `linked_ref_nav_page`                    | `link`, `url`, `href`, `route`                                                                        |
| Report column that opens the row's document (ADR-189)              | — (link cell)                        | `rowLink` (`column`/`route`/`idKey`)           | the report definition                                                                         | —                                                                                                     |
| QC inspector a QC call is assigned to (ADR-189)                    | `Assigned To`                        | `assignedTo`                                   | `qc_assignments.inspector_name` (QC Command)                                                  | `Inspector` — that is who SIGNED the inspection (`qcUserId` / `operatorName`)                         |
| A sales order's money totals (ADR-189)                             | `Subtotal` / `GST` / `Grand Total`   | `totals.subtotal` / `gstAmount` / `grandTotal` | computed server-side from lines × rate and `sales_orders.gst_percent`                         | `SO Value`, `Total Amount`                                                                            |
| An SO line's status seen from another document (ADR-189)           | `Line Status`                        | `lineStatus`                                   | `sales_order_lines.status`                                                                    | bare `Status` beside the SO's own status                                                              |

**Snapshot rule.** `xxxText` means "the value as it was, when the live row may be gone".
It is only ever a fallback: read `xxx ?? xxxText`, never the snapshot alone. A `xxxText`
with no live `xxx` to fall back from is a defect (C-13).

---

## B. Canonical dictionary — documents

Majority code scheme: **`IN-<ABBR>-#####`** — `IN-` prefix, uppercase abbreviation,
one hyphen, five zero-padded digits.

| Document                         | Label                           | Code                                      | Conforms                                           |
| -------------------------------- | ------------------------------- | ----------------------------------------- | -------------------------------------------------- |
| Sales Order                      | `SO`                            | `IN-SO-#####`                             | yes                                                |
| Job Work Sales Order             | `JWSO`                          | `IN-JW-#####`                             | prefix disagrees with the label                    |
| Job Card                         | `JC`                            | `IN-JC-YY-#####`                          | extra year segment; only series that resets yearly |
| Plan                             | `PLN`                           | `PLN-####`                                | no `IN-`, 4 digits                                 |
| **Production Order**             | `Production Order` — never `PO` | `IN-PRO-#####`                            | yes                                                |
| **Purchase Order (to a vendor)** | `PO`                            | `IN-MPO/JWPO/OPO/SPO-#####`               | 4 live prefixes for one document                   |
| Purchase Request                 | `PR`                            | `IN-PR-#####` (`IN-JWPR-` from an OSP op) | yes                                                |
| GRN                              | `GRN`                           | `IN-GRN-#####`                            | yes                                                |
| Delivery Challan                 | `DC`                            | `IN-DC-#####`                             | shares its series with the NC return challan       |
| JW Delivery Challan              | `JW DC`                         | `JWDC-OUT-####` / `JWIN-####`             | no `IN-`; the two directions use different shapes  |
| Non-Conformance                  | `NC`                            | typed by hand; auto ones `NC-AUTO-…`      | no series                                          |
| CAPA                             | `CAPA`                          | `CAPA-####`                               | no `IN-`, 4 digits                                 |
| Invoice                          | `INV`                           | `INV-####`                                | no `IN-`, 4 digits                                 |
| JW Invoice                       | `JW Invoice`                    | `IN-JWINV-#####`                          | yes                                                |
| Customer Dispatch                | `DSP`                           | `DSP-####`                                | no `IN-`, 4 digits                                 |
| Route Card                       | `RC`                            | `IN-RC-#####`                             | yes                                                |
| BOM                              | `BOM`                           | `BOM-####`                                | no `IN-`, 4 digits                                 |
| Store Issue / Tool Issue         | `ISS` / `TIS`                   | `ISS-#####` / `TIS-#####`                 | no `IN-`                                           |
| Party GRN                        | `PGRN`                          | `PGRN-#####`                              | no `IN-`, while its sibling GRN has it             |
| Design Tracker                   | `DSN`                           | `DSN-####`                                | no `IN-`, 4 digits                                 |
| Task                             | `TSK` / `TODO`                  | `TSK-####` / `TODO-####`                  | two prefixes for one document                      |

---

## C. What is still ambiguous — ranked, with the verdict

Every row is one decision. None is fixed yet. Counts are live occurrences.

| #   | Fault                                                                                                                                                                                                   | Evidence                                                                                                                                                                                            | Verdict                                                                                                                |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| 1   | **Four bare column headers name the row's subject, not the fact** — `Status` on 61 tables, `Date` 35, `Type` 27, `Qty` 22. `Date` alone covers 17 different dates; `Qty` covers 12 different quantities | `<th>Date</th>` at `jw-invoices/components/jw-invoice-view.tsx:113` (invoice date), `op-log/routes/list.tsx:205` (log date), `capa/components/capa-view.tsx:179` (CAPA date)                        | Qualify every one: `GRN Date`, `Issue Qty`, `PO Status`. A bare header is only legal where the table has one such fact |
| 2   | **`Rev` labels four different revisions on screen** — customer drawing, route card, BOM, design                                                                                                         | `sales-orders/components/so-drawing-history.tsx:177`, `route-cards/routes/list.tsx:193`, `bom-master/routes/detail.tsx:290`, `design-tracker/routes/list.tsx:152`                                   | Ban the bare word. `Rev` only inside `CODE/REV`; elsewhere say which revision                                          |
| 3   | **`revision` names six facts in the contracts** — SO line, JWSO line, item master, BOM counter, BOM log row, `/R` code suffix                                                                           | `sales-order.ts:64`, `job-work-order.ts:55`, `item.ts:19`, `bom-master.ts:31`, `bom-master.ts:74`, `doc-number.ts:100`                                                                              | `revision` only on the row that OWNS it; joined in, it is `itemRevision`                                               |
| 4   | **The sales-order number appears under 17 labels**, the job-card number under 11                                                                                                                        | `SO #` `assembly/routes/list.tsx:151`, `Order No` `so-planning/routes/workflow.tsx:1257`, `SO ref` `plans/routes/detail.tsx:275`, `SO/JC Ref` `daily-task-reports/components/report-modals.tsx:138` | `SO No.` and `JC No.` everywhere. A combined column is `SO / JWSO`, never a new name                                   |
| 5   | **`Sr No` means two different things** across ~20 screens — a display row counter, and the stored operation sequence                                                                                    | counter `vendors/routes/list.tsx:344`; real field `route-cards/routes/detail.tsx:240`                                                                                                               | `Sr No` = the counter only. The operation is `Op`                                                                      |
| 6   | **One quantity, many words** — produced qty under 7 labels, rejected 7, JC ordered 7, pending 5                                                                                                         | `Done` `machine-loading/routes/list.tsx:471` vs `Produced` `so-status/components/so-status-detail.tsx:506` vs `Qty Made Here` `op-entry/components/machine-op-entry-view.tsx:596`                   | Use section A. One word per quantity, everywhere                                                                       |
| 7   | **`Client` and `Customer` both label the same entity** — 8 screens vs 9, plus `Party` and `Bill to / Client`                                                                                            | `sales-orders/components/sales-order-form.tsx:728` vs `so-overview/routes/list.tsx:284` vs `production-orders/routes/detail.tsx:155`                                                                | `Customer` on screen; `client*` stays the field name                                                                   |
| 8   | **One column, two contract names** — `qc_accepted_qty` ships as `acceptedQty` on one screen, `qcAcceptedQty` on another                                                                                 | `apps/api/src/modules/incoming-qc/service.ts:403` and `:526`                                                                                                                                        | `qcAcceptedQty` wins; it says which acceptance                                                                         |
| 9   | **`availableQty` names four balances**, and the label `Available` three                                                                                                                                 | `op-entry.ts:431`, `store-inventory.ts:21`, `so-planning.ts:149`, `job-card.ts:491`                                                                                                                 | Legal only with the grain stated: `ospAvailableQty`, `freeStockQty`, …                                                 |
| 10  | **`orderQty` names four quantities** — SO line, plan's view, JC batch, production order (really `plan_qty`)                                                                                             | `sales-order.ts:71`, `plan.ts:76`, `job-card.ts:293`, `production-order.ts:64`                                                                                                                      | JC one becomes `batchQty`; production-order one becomes `planQty`                                                      |
| 11  | **`date` as a field name — six facts**, each aliased off a differently-named column                                                                                                                     | `customer-dispatch.ts:123`, `daily-report.ts:53`, `job-card.ts:450`, `so-timeline.ts:29`, `so-qc-status.ts:95`, `global-search.ts:135`                                                              | A field called `date` is banned. Name the event                                                                        |
| 12  | **Item name has three DB columns** — `item_name`, `part_name`, `item_name_text`                                                                                                                         | `schema.ts:1468`, `:1816`, `:2461`                                                                                                                                                                  | No migration. Every contract exposes it as `itemName`                                                                  |
| 13  | **`…Text` snapshots with nothing to fall back from** — `rawMaterialGradeText`, `rawMaterialSizeText` (14 uses each), `partText`                                                                         | `route-card.ts:54`, `design-project.ts:139`                                                                                                                                                         | Drop the suffix — they are plain values, not snapshots                                                                 |
| 14  | **Document codes run two schemes** — `IN-SO-00417` against `PLN-0005`, `CAPA-0001`, `DSP-0004`                                                                                                          | section B                                                                                                                                                                                           | Leave live series alone; new documents follow `IN-<ABBR>-#####`                                                        |
| 15  | **JWSO is labelled JWSO but numbered `IN-JW-`**                                                                                                                                                         | `apps/api/src/modules/job-work-orders/service.ts:143`                                                                                                                                               | Live codes stay; fix only if the series is ever reset                                                                  |
| 16  | **The NC return-to-vendor challan draws from the DC series** — two documents, one number line                                                                                                           | `apps/api/src/modules/nc-register/service.ts:1530`                                                                                                                                                  | Acceptable only if it truly is the same document type. Decide and write it down                                        |
| 17  | **Two whole contracts are dead** — `qc-dashboard.ts` (no route serves it) and `service-po.ts` (table exists, no service)                                                                                | `packages/shared/src/schemas/`, `apps/web/src/modules/qc-command/routes/index.tsx:5`                                                                                                                | Delete, or mark planned with a date                                                                                    |
| 18  | **`POL` also names a vendor-PO-line link in code**                                                                                                                                                      | `packages/shared/src/schemas/jc-op-po-line.ts:3`                                                                                                                                                    | Rename that one `outsourcePoLineId`. On screen `POL` is the customer's line, always                                    |

Checked and cleared, so they are not re-raised: `PO` reads ambiguously in code but the
Production Orders screen says "Production Order No" in full; `WO` is not an entity at all
and survives only in the co-label "SO / WO"; `FI` appears nowhere — the system spells out
"Final Inspection"; `items.revision` is deliberately shown on no screen.

---

## D. How to add a name

1. Look for the fact in section A or B. If it is there, use that name. There is no second
   name for it.
2. If it is not there, add a row to A or B in the same commit as the code.
3. A label may be shortened only if the short form is unique across the whole app.
   `POL` qualified; `Rev` does not.
4. A column header must name the FACT, not the row's subject. `Date` is not a fact.
