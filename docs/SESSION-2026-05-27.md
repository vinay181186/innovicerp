# Session Log — 2026-05-27

> Quick-check record of this session's work + tomorrow's resume points.
> All commits below are on `main` and **pushed** to `origin` (tip `4f3ccb7`).
> Deeper detail: `docs/PARITY/qc-build-tracker.md`, `docs/PARITY/print-templates.md`, `docs/TASKS.md`.

---

## Part A — Print Templates P2 (earlier today)
Real template-consuming prints on the docs vendors receive.
- `1f2b66f` — PO / OSP-DC / JW-DC prints consuming the editor templates (shared `@/lib/print/doc-print.ts` + `company.ts`; new `jw-dc` detail page). 8/8 web unit tests.

## Part B — QC module legacy-parity rebuild (main work)
Goal: map every QC page from the legacy HTML, find all remaining differences, build to 1:1. A 4-agent re-audit of all 13 QC pages found real gaps beyond the old "13/13 complete" state. **12 build chunks, 9 commits.** Decisions taken (AskUserQuestion): full QC report attachments · QC Documents rebuilt to the legacy SO-pivoted matrix · keep std-time in minutes · add NC operator field + 'General' shift.

| Commit | What |
|---|---|
| `0c8567f` | Schema foundation — migrations **0043** (NC `operator_text`; `op_log` + GRN-line `qc_report_path`/`_name`; `qc_documents` matrix cols) + **0044** (`shift` += `general`). Applied to dev DB. |
| `f23dc4c` | Day/Night/**General** shift across Op-Entry / QC Call Register / TPI |
| `3985aa8` | QC Command Center — **Pareto** + **Inspector Performance** rebuilt into real legacy reports |
| `fcf4f2b` | **NC Register** (5 cards, reason filter, operator, JC-op dropdown, dispose panel, NC↔CAPA) + **CAPA** (NC picker, Responsible picker, Monitoring) + **QC Reports** (disposition breakdown + NC-by-SO/JC + NC-all) |
| `568a305` | **Incoming QC** value-in-QC card + **TPI** Excel export + QC-History/Call-Register backend (clientPoLineNo/qcCallDate/logNo + blink css + export lib) |
| `42c1a20` | **QC History** UI (link-fix, blink, export, IST dates) + **QC Call Register** UI (CPO/logNo/Called→Response/COMPLETE/search/datalist) + **SO QC Status** full rebuild (per-op cells, % bar, expandable GRN/TPI/Docs, totals, Due/Type) |
| `7d2f1a5` | **QC report attachments** — upload on 4 QC/TPI/GRN submit forms + ⬇ Report link on 5 view surfaces (shared `qc-report-attach.tsx`) |
| `2931be8` | **QC Documents** rebuilt → legacy SO-pivoted completion **matrix** (dynamic QC-op cols, serial ranges, mandatory/optional gating, line-detail modal, Export/Download-All) |
| `f2bd41a` | QC History Report column (chunk-2 follow-up) |
| `28c30f8` | docs(tasks) — QC complete |

**Verification:** all packages typecheck + lint clean; affected QC test suites pass.

## Part C — Print Templates P3 (after QC)
Fixed-layout prints via shared `apps/web/src/lib/print/print-window.ts` (legacy `printWindow` L10524). 6 of 7 built.
| Commit | What |
|---|---|
| `6ef78a2` | shared `printWindow` util + **Route Card** print |
| `3338472` | **Machine Queue**, **Daily Report**, **Dispatch Register**, **Drawing**, **Job Card** prints |
| `4f3ccb7` | docs(print-templates) — P3 done |
- ⏸ **Invoice print DEFERRED** — no React invoice/tax-invoice module exists yet.

---

## ⏭ Resume tomorrow from here
1. **QC end-to-end test (user).** Before runtime-testing, confirm the dev/test DB has migrations **0043 + 0044** applied (they were applied this session via `apply-sql.ts`) and the GRN cascade deps. Click through: QC Command Center (Pareto/Inspector), SO QC Status (per-op cells + expandable rows), QC Documents matrix, report attach/download on the 5 surfaces, NC↔CAPA, QC Reports.
2. **Known PRE-EXISTING env test failures (NOT from this work — verified by stashing):**
   - `goods-receipt-notes` service/routes tests — dev-DB create→read cascade ("GRN not found"). Re-test on a clean/seeded DB.
   - `reports` `items-on-hand` — an item has negative computed on-hand in the dev DB.
   - `qc-command` service+routes collide on a shared `testOpId` under vitest file-parallelism → **run per-file**.
3. **Documented DELTAs** (deferred, in the tracker): Job Card omits Drawing No./Material/Program/Tool No./Production-Log; Dispatch Register is header-grain; QC Docs JW-selector deferred; Inspector "Avg Hrs" dropped (no hours column); std-time stays minutes; pickup/add RBAC kept tighter than legacy.
4. **Open follow-ups:** Invoice print (needs an invoice module); next ERP section parity sweep (TBD).
