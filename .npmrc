# engine-strict=false so CI/CD images with an older Node (e.g. Cloudflare Pages'
# build image, which tops out below the repo's declared Node 24) don't abort the
# install on an engine check. The web build does not actually need Node 24.
# See docs/GO_LIVE.md Phase 2, step 6.
engine-strict=false
